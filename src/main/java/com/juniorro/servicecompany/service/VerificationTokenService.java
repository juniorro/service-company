/*package com.juniorro.servicecompany.service;

import org.springframework.stereotype.Service;

import com.juniorro.servicecompany.model.SystemUser;
import com.juniorro.servicecompany.model.security.VerificationToken;

@Service
public interface VerificationTokenService {

	VerificationToken findByToken(final String token);

	void createVerificationToken(SystemUser systemUser, String token);
	
	void saveToken(VerificationToken myToken);

	VerificationToken getVerificationToken(String token);

	void delete(final long id);
	
}
*/